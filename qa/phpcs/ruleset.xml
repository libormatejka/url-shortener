<?xml version="1.0"?>
<ruleset name="collectorboy_cz">

    <rule ref="Generic.Arrays.ArrayIndent"/>
    <rule ref="Generic.Arrays.DisallowLongArraySyntax"/>
    <rule ref="Generic.Classes.DuplicateClassName"/>
    <rule ref="Generic.ControlStructures.DisallowYodaConditions"/>
    <rule ref="Generic.Files.EndFileNewline"/>
    <rule ref="Generic.Functions.FunctionCallArgumentSpacing"/>
    <rule ref="Generic.Functions.OpeningFunctionBraceBsdAllman"/>
    <rule ref="Generic.PHP.CharacterBeforePHPOpeningTag"/>
    <rule ref="Generic.PHP.Syntax"/>
    <rule ref="Generic.WhiteSpace.DisallowSpaceIndent"/>
    <rule ref="Generic.WhiteSpace.DisallowSpaceIndent"/> <!-- Required tabs -->
    <rule ref="Generic.WhiteSpace.ScopeIndent"/>

    <rule ref="Squiz.Classes.ClassDeclaration"/>
    <rule ref="Squiz.Classes.LowercaseClassKeywords"/>
    <rule ref="Squiz.WhiteSpace.SemicolonSpacing"/>
    <rule ref="Squiz.WhiteSpace.SuperfluousWhitespace"/>

    <config name="installed_paths" value="../../slevomat/coding-standard"/>

    <rule ref="SlevomatCodingStandard.Arrays.DisallowImplicitArrayCreation"/> <!-- Disallows implicit array creation. -->
    <rule ref="SlevomatCodingStandard.Arrays.MultiLineArrayEndBracketPlacement"/> <!-- Enforces reasonable end bracket placement for multi-line arrays. -->
    <rule ref="SlevomatCodingStandard.Arrays.SingleLineArrayWhitespace"/> <!-- Checks whitespace in single line array declarations (whitespace between brackets, around commas, ...). -->
    <rule ref="SlevomatCodingStandard.Arrays.TrailingArrayComma"/> <!-- Commas after last element in an array make adding a new element easier and result in a cleaner versioning diff. -->
    <rule ref="SlevomatCodingStandard.Classes.ClassConstantVisibility"/> <!-- In PHP 7.1+ it's possible to declare visibility of class constants. In a similar vein to optional declaration of visibility for properties and methods which is actually required in sane coding standards, this sniff also requires declaring visibility for all class constants. -->
    <rule ref="SlevomatCodingStandard.Classes.ClassMemberSpacing"/> <!-- Checks lines count between different class members, eg. between last property and first method. -->
    <rule ref="SlevomatCodingStandard.Classes.ModernClassNameReference"/> <!-- Reports use of __CLASS__, get_parent_class(), get_called_class(), get_class() and get_class($this). Class names should be referenced via ::class constant when possible. -->
    <rule ref="SlevomatCodingStandard.Classes.EmptyLinesAroundClassBraces"/> <!-- Enforces one configurable number of lines after opening class/interface/trait brace and one empty line before the closing brace. -->
    <rule ref="SlevomatCodingStandard.ControlStructures.NewWithParentheses"/>
    <rule ref="SlevomatCodingStandard.Files.TypeNameMatchesFileName"> <!-- For projects not following the PSR-0 or PSR-4 autoloading standards, this sniff checks whether a namespace and a name of a class/interface/trait follows agreed-on way to organize code into directories and files. -->
        <properties>
            <property name="rootNamespaces" type="array">
                <element key="app" value="App"/>
                <element key="tests" value="Tests"/>
            </property>
        </properties>
    </rule>
    <rule ref="SlevomatCodingStandard.Functions.RequireTrailingCommaInCall"/> <!-- Commas after the last parameter in function or method call make adding a new parameter easier and result in a cleaner versioning diff. -->
    <rule ref="SlevomatCodingStandard.Functions.RequireTrailingCommaInDeclaration"/> <!-- Commas after the last parameter in function or method declaration make adding a new parameter easier and result in a cleaner versioning diff. -->
    <rule ref="SlevomatCodingStandard.Classes.MethodSpacing"/> <!-- Checks that there is a certain number of blank lines between methods. -->
    <rule ref="SlevomatCodingStandard.Classes.PropertySpacing"/> <!-- Checks that there is a certain number of blank lines between properties. -->
    <rule ref="SlevomatCodingStandard.Namespaces.AlphabeticallySortedUses"/> <!-- Checks whether uses at the top of a file are alphabetically sorted. -->
    <rule ref="SlevomatCodingStandard.Namespaces.DisallowGroupUse"/> <!-- Group use declarations are ugly, make diffs ugly and this sniff prohibits them. -->
    <rule ref="SlevomatCodingStandard.Namespaces.MultipleUsesPerLine"/> <!-- Prohibits multiple uses separated by commas. -->
    <rule ref="SlevomatCodingStandard.Namespaces.NamespaceDeclaration"/> <!-- Enforces one space after namespace, disallows content between namespace name and semicolon and disallows use of bracketed syntax. -->
    <rule ref="SlevomatCodingStandard.Namespaces.NamespaceSpacing"/> <!-- Enforces configurable number of lines before and after namespace. -->
    <rule ref="SlevomatCodingStandard.Namespaces.RequireOneNamespaceInFile"/> <!-- Requires only one namespace in a file. -->
    <rule ref="SlevomatCodingStandard.Namespaces.UnusedUses"> <!-- Looks for unused imports from other namespaces. -->
        <properties>
            <property name="searchAnnotations" type="boolean" value="true"/>
        </properties>
    </rule>
    <rule ref="SlevomatCodingStandard.Namespaces.UseDoesNotStartWithBackslash"/> <!-- Disallows leading backslash in use statement.-->
    <rule ref="SlevomatCodingStandard.Namespaces.UseFromSameNamespace"/> <!-- Prohibits uses from the same namespace. -->
    <rule ref="SlevomatCodingStandard.Namespaces.UseSpacing"/> <!-- Enforces configurable number of lines before first use, after last use and between two different types of use (eg. between use function and use const). Also enforces zero number of lines between same types of use. -->
    <rule ref="SlevomatCodingStandard.Namespaces.UselessAlias"/> <!-- Looks for use alias that is same as unqualified name. -->
    <rule ref="SlevomatCodingStandard.PHP.DisallowDirectMagicInvokeCall"/>
    <rule ref="SlevomatCodingStandard.PHP.DisallowReference"/>
    <rule ref="SlevomatCodingStandard.PHP.OptimizedFunctionsWithoutUnpacking"/>
    <rule ref="SlevomatCodingStandard.PHP.ReferenceSpacing"/>
    <rule ref="SlevomatCodingStandard.PHP.RequireExplicitAssertion"/>
    <rule ref="SlevomatCodingStandard.PHP.RequireNowdoc"/>
    <rule ref="SlevomatCodingStandard.PHP.ShortList"/>
    <rule ref="SlevomatCodingStandard.PHP.TypeCast"/> <!-- Enforces using shorthand cast operators, forbids use of unset and binary cast operators: (bool) instead of (boolean), (int) instead of (integer), (float) instead of (double) or (real). (binary) and (unset) are forbidden. -->
    <rule ref="SlevomatCodingStandard.PHP.UselessParentheses"/>
    <rule ref="SlevomatCodingStandard.PHP.UselessSemicolon"/>
    <rule ref="SlevomatCodingStandard.TypeHints.DeclareStrictTypes">
        <properties>
            <property name="declareOnFirstLine" type="boolean" value="true"/>
        </properties>
    </rule>
    <rule ref="SlevomatCodingStandard.TypeHints.DisallowArrayTypeHintSyntax"/>
    <rule ref="SlevomatCodingStandard.TypeHints.DisallowMixedTypeHint"/>
    <rule ref="SlevomatCodingStandard.TypeHints.LongTypeHints"/> <!-- Enforces using shorthand scalar typehint variants in phpDocs: int instead of integer and bool instead of boolean. This is for consistency with native scalar typehints which also allow shorthand variants only. -->
    <rule ref="SlevomatCodingStandard.TypeHints.NullTypeHintOnLastPosition"/> <!-- Enforces null type hint on last position in annotations. -->
    <rule ref="SlevomatCodingStandard.TypeHints.NullableTypeForNullDefaultValue"/>
    <rule ref="SlevomatCodingStandard.TypeHints.ParameterTypeHint"/>
    <rule ref="SlevomatCodingStandard.TypeHints.ParameterTypeHintSpacing"/>
    <rule ref="SlevomatCodingStandard.TypeHints.PropertyTypeHint"/>
    <rule ref="SlevomatCodingStandard.Classes.PropertyDeclaration"/>
    <rule ref="SlevomatCodingStandard.TypeHints.ReturnTypeHint"/>
    <rule ref="SlevomatCodingStandard.TypeHints.ReturnTypeHintSpacing"/>
    <rule ref="SlevomatCodingStandard.TypeHints.DNFTypeHintFormat"/> <!-- Checks format of union type hints. -->
    <rule ref="SlevomatCodingStandard.TypeHints.UselessConstantTypeHint"/> <!-- Reports useless @var annotation (or whole documentation comment) for constants because the type of constant is always clear. -->
    <rule ref="SlevomatCodingStandard.Whitespaces.DuplicateSpaces"/> <!-- Checks duplicate spaces anywhere because there aren't sniffs for every part of code to check formatting. -->

</ruleset>
